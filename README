Get Repo

    mkdir ~/bin
    export PATH=~/bin:$PATH
    curl https://dl-ssl.google.com/dl/googlesource/git-repo/repo > ~/bin/repo
    chmod a+x ~/bin/repo

Sync RootBox Sources

    repo init -u https://github.com/Mustaavalkosta/android.git -b rootbox
    repo sync
